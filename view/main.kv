#: include view/npo.kv
#: include view/custom.kv
#: import NoTransition kivy.uix.screenmanager.NoTransition
#: import os.path os.path

WindowManager:
    transition: NoTransition()
    id: screen_manager

    #current: "first" if os.path.isfile("./data/download_path.csv") else "file_npo"
    #NPO Windows

    NpoFileWindow:
        id: npo_fifth_screen
        name: "file_npo"
        manager: screen_manager
    NpoWindow:
        id: first_screen
        manager: screen_manager
    NpoCreateWindow:
        id: npo_second_screen
        manager: screen_manager
    NpoDeleteWindow:
        id: npo_third_screen
        manager: screen_manager
    NpoUpdateWindow:
        id: npo_fourth_screen
        manager: screen_manager
